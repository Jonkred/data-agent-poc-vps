name: 'poc_dw'
version: '1.0.0'
config-version: 2

profile: 'poc_dw'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  poc_dw:
    bronze:
      +materialized: table
      +schema: poc_dw
    silver:
      +materialized: table
      +schema: poc_dw
    gold:
      +materialized: table
      +schema: poc_dw
